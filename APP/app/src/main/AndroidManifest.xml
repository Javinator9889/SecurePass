<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="javinator9889.securepass">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".SecurePass"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".views.activities.LauncherActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".views.activities.FirstSetup"
            android:label="AppIntro"/>
        <activity android:name=".network.drive.CreateFileInAppFolder" />
        <activity android:name=".network.drive.RetrieveContentWithDownloadProgress" />
        <activity android:name=".views.activities.ShowEulaActivity" />
        <activity android:name=".views.fragments.PasswordRegistration" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>
